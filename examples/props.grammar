source
	| statement*

statement
	| assignment
	| impure_statement

assignment
	| ident typedef? = expr

expr
	| math_expr
	| func

func
	| parameters { statement* }

math_expr
	| number
	| math_expr + math_expr
	| math_expr - math_expr
	| math_expr * math_expr
	| math_expr / math_expr
	| math_expr % math_expr

typedef
	| (ident*)

parameters
	| |ident|

ident
	| [a-z|A-Z|0-9]+

